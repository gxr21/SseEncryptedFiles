
import './green.css'; // Optional: for custom CSS

const Green = ({ children, onClick, variant = 'primary', disabled = false }) => {
  return (
    <button className="green" >GREEN</button>
  );
};

export default Green;
