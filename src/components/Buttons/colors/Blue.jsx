
import './Blue.css'; // Optional: for custom CSS

const Blue = ({ children, onClick, variant = 'primary', disabled = false }) => {
  return (
    <button className="blue" >BLUE</button>
  );
};

export default Blue;
